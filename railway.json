{
  "build": {
    "builder": "railpack",
    "rootDirectory": "budgetin-aja"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "port": 8080
  }
}
